#pragma once
#include<string>
#include<iostream>
#include<vector>

long long inputData(const std::string& mess = "Enter number: ");

long long binarySearch(const std::vector<long long>& arr, const long long element, const long long l, const long long r);